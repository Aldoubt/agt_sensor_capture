diff --git a/CMakeLists.txt b/CMakeLists.txt
index 850b08f..f5a0f66 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -10,9 +10,14 @@ else(ROS_EDITION STREQUAL "ROS2")
     set(CMAKE_C_STANDARD 99)
   endif()
 
-  # Default to C++14
+  # AGT timebase uses C++17 library interfaces.
   if(NOT CMAKE_CXX_STANDARD)
-    set(CMAKE_CXX_STANDARD 14)
+    set(CMAKE_CXX_STANDARD 17)
+  endif()
+
+  # This AGT pin targets ROS 2 Humble unless a distro is supplied explicitly.
+  if(NOT DISTRO_ROS)
+    set(DISTRO_ROS "humble")
   endif()
 
   list(INSERT CMAKE_MODULE_PATH 0 "${PROJECT_SOURCE_DIR}/cmake/modules")
diff --git a/package_ROS2.xml b/package_ROS2.xml
index cb8e1ff..8a99b05 100644
--- a/package_ROS2.xml
+++ b/package_ROS2.xml
@@ -8,6 +8,7 @@
   <depend>pcl_conversions</depend>
   <depend>rcl_interfaces</depend>
   <depend>libpcl-all-dev</depend>
+  <depend>agt_timebase</depend>
 
   <exec_depend>rosbag2</exec_depend>
   <exec_depend>rosidl_default_runtime</exec_depend>
diff --git a/src/lddc.cpp b/src/lddc.cpp
index f39299a..6778b0c 100644
--- a/src/lddc.cpp
+++ b/src/lddc.cpp
@@ -2,6 +2,8 @@
 #include "comm/ldq.h"
 #include "comm/comm.h"
 
+#include <chrono>
+
 #include <inttypes.h>
 #include <iostream>
 #include <iomanip>
@@ -26,6 +28,7 @@ Lddc::Lddc(int format, int multi_topic, int data_src, int output_type,
       frame_id_(frame_id) {
   publish_period_ns_ = kNsPerSecond / publish_frq_;
   lds_ = nullptr;
+  shared_timebase_writer_ = std::make_unique<agt_timebase::SharedTimebaseWriter>();
 #if 0
   bag_ = nullptr;
 #endif
@@ -44,6 +47,13 @@ void Lddc::PublishCustomPointcloud(LidarDataQueue *queue, uint8_t index) {
     CustomMsg livox_msg;
     InitCustomMsg(livox_msg, pkg, index);
     FillPointsToCustomMsg(livox_msg, pkg);
+    if (shared_timebase_writer_) {
+      const auto host_steady_ns = std::chrono::duration_cast<std::chrono::nanoseconds>(
+          std::chrono::steady_clock::now().time_since_epoch()).count();
+      shared_timebase_writer_->write(
+          static_cast<uint64_t>(pkg.base_time),
+          static_cast<uint64_t>(host_steady_ns));
+    }
     PublishCustomPointData(livox_msg, index);
   }
 }
diff --git a/src/lddc.h b/src/lddc.h
index 1b2a354..c50aa19 100644
--- a/src/lddc.h
+++ b/src/lddc.h
@@ -6,6 +6,11 @@
 #include "driver_node.h"
 #include "lds.h"
 
+#ifdef BUILDING_ROS2
+#include <memory>
+#include "agt_timebase/shared_timebase.hpp"
+#endif
+
 namespace livox_ros {
 
 /** Send pointcloud message Data to ros subscriber or save them in rosbag file */
@@ -18,6 +23,7 @@ class Lddc final {
   PublisherPtr global_pub_;
   PublisherPtr private_imu_pub_[kMaxSourceLidar];
   PublisherPtr global_imu_pub_;
+  std::unique_ptr<agt_timebase::SharedTimebaseWriter> shared_timebase_writer_;
 #endif
 
   livox_ros::DriverNode *cur_node_;
